// Тег кешу публічної витрини. Дані каталогу кешуються на сервері, а адмінка
// після кожної зміни скидає саме цей тег — тож правка з'являється на сайті
// одразу, а не за розкладом.
export const PUBLIC_TAG = "public";

/**
 * Скільки кеш живе без явного скидання. Це страховка на випадок, якщо
 * інвалідація з адмінки не спрацювала: тоді зміна з'явиться щонайпізніше через
 * хвилину, а не висітиме довго. Навантаження мізерне — максимум один похід
 * у БД на хвилину незалежно від кількості відвідувачів.
 */
export const PUBLIC_REVALIDATE = 60;
